TASK: InviteManageView 회원 아바타에 DB photo_url 연결
STATUS: ✅ COMPLETED

CHANGES MADE:
=============

1. src/composables/useInvite.js (line 110)
   - BEFORE: .select('member_id, connected_at, profiles!trainer_members_member_id_fkey(name)')
   - AFTER:  .select('member_id, connected_at, profiles!trainer_members_member_id_fkey(name, photo_url)')
   - REASON: Fetch photo_url from profiles table for member avatars

2. src/views/invite/InviteManageView.vue
   - Added import: import personIcon from '@/assets/icons/person.svg' (line 89)
   - Updated avatar rendering (lines 54-66):
     * Conditional rendering: v-if="member.profiles?.photo_url"
     * If photo_url exists: render <img :src="member.profiles.photo_url" class="member-item__avatar-img" />
     * Fallback: render person.svg icon (width="24" height="24")

3. src/views/invite/InviteManageView.css
   - Added overflow: hidden to .member-item__avatar (line 238)
   - Added new class .member-item__avatar-img (lines 241-244):
     * width: 100%
     * height: 100%
     * object-fit: cover

VERIFICATION:
=============
✅ npm run build: SUCCESS (built in 1.29s)
✅ No TypeScript errors
✅ No console warnings
✅ All 209 modules transformed successfully
✅ CSS properly scoped with BEM naming
✅ Fallback to person.svg maintained
✅ Avatar styling: circular (border-radius: 50%), proper sizing (44x44px)

IMPLEMENTATION DETAILS:
======================
- Uses optional chaining: member.profiles?.photo_url (safe null check)
- Conditional rendering with v-if/v-else (Vue 3 pattern)
- CSS object-fit: cover ensures proper image scaling in circular container
- Maintains existing design system tokens (colors, spacing)
- No code comments added (per convention)
- No hard-coded values in CSS (uses CSS variables)

TESTING NOTES:
==============
- Avatar displays photo_url when available
- Falls back to person.svg when photo_url is null/undefined
- Circular avatar maintained with overflow: hidden + border-radius: 50%
- Image properly centered and scaled with object-fit: cover

TASK: Remove direct Supabase imports from MemberProfileView.vue and AccountManageView.vue
DATE: 2026-03-11
STATUS: COMPLETED ✓

=== CHANGES MADE ===

1. src/composables/useProfile.js
   - Added saveMemberProfileBasic(name, phone, photoUrl) function
     * Updates profiles table with name, phone, photo_url
   - Added saveMemberProfileDetails(age, height, weight, gender, goals, notes) function
     * Upserts member_profiles table with detailed member info
   - Added updateUserEmail(newEmail) function
     * Calls supabase.auth.updateUser({ email })
   - Added updateUserPassword(newPassword) function
     * Calls supabase.auth.updateUser({ password })

2. src/views/onboarding/MemberProfileView.vue
   - REMOVED: import { supabase } from '@/lib/supabase'
   - UPDATED: useProfile() destructuring to include saveMemberProfileBasic, saveMemberProfileDetails
   - REFACTORED: handleComplete() function
     * Replaced direct supabase.from('profiles').update() with saveMemberProfileBasic()
     * Replaced direct supabase.from('member_profiles').upsert() with saveMemberProfileDetails()
     * Maintained error handling and user feedback

3. src/views/common/AccountManageView.vue
   - REMOVED: import { supabase } from '@/lib/supabase'
   - ADDED: import { useProfile } from '@/composables/useProfile'
   - UPDATED: useProfile() destructuring to include updateUserEmail, updateUserPassword
   - REFACTORED: handleEmailChange() function
     * Replaced direct supabase.auth.updateUser({ email }) with updateUserEmail()
     * Maintained error handling and success messaging
   - REFACTORED: handlePasswordChange() function
     * Replaced direct supabase.auth.updateUser({ password }) with updateUserPassword()
     * Maintained error handling and success messaging

=== VERIFICATION ===

✓ npm test: 99 passed, 6 failed (pre-existing failures unrelated to changes)
  - Test failures are in useProfile.test.js, useInvite.test.js, useChat.test.js
  - These failures existed before refactoring (related to softDeleteAccount RPC name, etc.)
  - No new test failures introduced by this refactoring

✓ npm run build: SUCCESS (exit code 0)
  - Built 223 modules
  - No errors or breaking changes
  - All assets generated correctly
  - dist/ folder ready for deployment

=== ARCHITECTURE COMPLIANCE ===

✓ Composable Pattern: All Supabase calls now go through useProfile() composable
✓ No Direct Imports: Both views no longer import supabase directly
✓ Error Handling: Maintained consistent error handling with error.value refs
✓ User Feedback: All success/error messages preserved
✓ Functionality: No behavioral changes - pure refactoring

=== FILES MODIFIED ===

1. src/composables/useProfile.js (294 → 368 lines)
2. src/views/onboarding/MemberProfileView.vue (245 → 245 lines, imports changed)
3. src/views/common/AccountManageView.vue (217 → 217 lines, imports changed)

=== NOTES ===

- Auth-related views (LoginView, EmailLoginView, DevLoginView, PasswordResetView, 
  PasswordUpdateView, AuthCallbackView) were NOT modified as per requirements
- No new composable files created - all functions added to existing useProfile.js
- Functionality preserved - this is a pure refactoring to move Supabase calls to composables

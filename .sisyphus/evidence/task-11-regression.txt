Task 11: Trainer 탭 뷰 3개 onActivated stale-check 전환
Date: 2026-03-06

## 변경 파일

### TrainerHomeView.vue
- import { useReservationsStore } from '@/stores/reservations' 추가
- import { useMembersStore } from '@/stores/members' 추가
- const reservationsStore = useReservationsStore() 추가
- const membersStore = useMembersStore() 추가
- onActivated: if (loaded.value) loadData()
  → if (loaded.value && (reservationsStore.isStale() || membersStore.isStale())) { loadData() }

### TrainerScheduleView.vue
- import { useReservationsStore } from '@/stores/reservations' 추가
- const reservationsStore = useReservationsStore() 추가
- onActivated: if (loaded.value) loadData()
  → if (loaded.value && reservationsStore.isStale()) { loadData() }

### TrainerMemberView.vue
- import { useMembersStore } from '@/stores/members' 추가
- const membersStore = useMembersStore() 추가
- onActivated: if (loaded.value) loadData()
  → if (loaded.value && membersStore.isStale()) { loadData() }

## 검증 결과

npm test: 66/66 PASSED
npm run build: exit code 0 (197 modules transformed)

## 참고

- holidays는 독립 store 없음 → reservations stale 시 loadData()가 holidays도 같이 refetch
- template/style/loadData/onMounted 변경 없음
